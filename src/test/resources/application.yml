spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: SA
    password:
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        highlight_sql: true
        format_sql: true
